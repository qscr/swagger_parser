enum ParameterType {
  integer,
  double,
  string,
  bool,
  array,
  object,
  file,
  number,
}

enum ParameterLocation {
  path,
  headers,
  query,
  body,
}

enum MethodType {
  get,
  post,
  put,
  delete,
}
